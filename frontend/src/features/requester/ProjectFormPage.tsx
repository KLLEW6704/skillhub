import{useMutation}from'@tanstack/react-query';import{useNavigate}from'react-router-dom';import type{FormEvent}from'react';import{apiRequest}from'../../lib/api';export function ProjectFormPage(){const n=useNavigate();const m=useMutation({mutationFn:(x:unknown)=>apiRequest('/api/v1/projects',{method:'POST',body:JSON.stringify(x)}),onSuccess:()=>n('/requester/projects')});function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));m.mutate({...d,budget:Number(d.budget),required_skills:String(d.required_skills).split(/[,，]/).map(x=>x.trim()).filter(Boolean)})}return <section className="workspace-page"><h1>发布项目</h1><form className="stack-form" onSubmit={submit}><label>项目名称<input name="title" required/></label><label>项目说明<textarea name="description" required/></label><label>分类<input name="category" required/></label><label>所需技能<input name="required_skills" placeholder="摄影，视频剪辑" required/></label><label>截止日期<input name="deadline" type="date" required/></label><label>预算参考<input name="budget" type="number" min="0"/></label><button className="primary-button">提交审核</button></form></section>}

import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{useParams}from'react-router-dom';import{apiRequest}from'../../lib/api';import type{Application}from'../../lib/types';export function ApplicantsPage(){const{id}=useParams();const qc=useQueryClient();const q=useQuery({queryKey:['applicants',id],queryFn:()=>apiRequest<Application[]>(`/api/v1/projects/${id}/applications`)});const m=useMutation({mutationFn:({aid,a}:{aid:number;a:string})=>apiRequest(`/api/v1/applications/${aid}/${a}`,{method:'POST'}),onSuccess:()=>qc.invalidateQueries({queryKey:['applicants',id]})});return <section className="workspace-page"><h1>申请者管理</h1>{q.data?.map(x=><article className="record-item" key={x.id}><div><b>学生 #{x.student_id}</b><p>{x.message}</p></div>{x.status==='pending'&&<div><button onClick={()=>m.mutate({aid:x.id,a:'accept'})}>录用</button><button onClick={()=>m.mutate({aid:x.id,a:'reject'})}>拒绝</button></div>}</article>)}</section>}

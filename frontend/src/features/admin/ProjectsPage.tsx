import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{apiRequest}from'../../lib/api';import{statusLabel}from'../../lib/status';import type{Project}from'../../lib/types';export function ProjectsPage(){const qc=useQueryClient();const q=useQuery({queryKey:['admin-projects'],queryFn:()=>apiRequest<Project[]>('/api/v1/admin/projects')});const m=useMutation({mutationFn:({id,a}:{id:number;a:string})=>apiRequest(`/api/v1/admin/projects/${id}/${a}`,{method:'POST'}),onSuccess:()=>qc.invalidateQueries({queryKey:['admin-projects']})});return <section className="workspace-page"><h1>项目审核</h1>{q.data?.map(p=><article className="manage-card" key={p.id}><div><b>{p.title}</b><p>{statusLabel(p.audit_status)} · {statusLabel(p.lifecycle_status)}</p></div>{p.audit_status==='pending'&&<div className="manage-actions"><button onClick={()=>m.mutate({id:p.id,a:'approve'})}>通过</button><button onClick={()=>m.mutate({id:p.id,a:'reject'})}>拒绝</button></div>}</article>)}</section>}

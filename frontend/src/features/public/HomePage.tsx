import { useQuery } from '@tanstack/react-query'
import { Link } from 'react-router-dom'
import { ProjectCard } from '../../components/ProjectCard'
import { apiRequest } from '../../lib/api'
import type { ProjectPage,StudentProfile } from '../../lib/types'
export function HomePage(){const projects=useQuery({queryKey:['home-projects'],queryFn:()=>apiRequest<ProjectPage>('/api/v1/projects?page_size=3')});const students=useQuery({queryKey:['home-students'],queryFn:()=>apiRequest<StudentProfile[]>('/api/v1/profiles/students')});return <><section className="home-hero"><p className="eyebrow">SKILL GROWTH ARCHIVE · 2026</p><h1>把技能写进<br/><em>真实发生的事</em>里</h1><p className="hero-copy">连接校园项目、作品证据与同行评价。不是一句“我会”，而是一份可展示、可验证、持续成长的技能档案。</p><div className="hero-actions"><Link className="primary-button" to="/projects">浏览实践项目</Link><Link className="secondary-button" to="/register">建立我的档案</Link></div><div className="growth-strip">{['创建技能','提交作品','参与项目','获得评价'].map((item,index)=><div key={item}><b>0{index+1}</b><span>{item}</span></div>)}</div></section><section className="home-section"><div className="section-title"><span>01</span><h2>正在招募</h2><Link to="/projects">查看全部 →</Link></div><div className="project-grid">{projects.data?.items.map((p,i)=><ProjectCard project={p} index={i+1} key={p.id}/>)}</div></section><section className="home-section paper-dark"><div className="section-title"><span>02</span><h2>成长中的学生</h2><Link to="/talent">浏览技能大厅 →</Link></div><div className="student-list">{students.data?.slice(0,4).map(s=><Link to={`/talent/${s.user_id}`} key={s.user_id}><b>{s.display_name}</b><span>{s.skills.map(k=>k.name).join(' · ')||'正在建立技能档案'}</span></Link>)}</div></section></>}

import type { Review } from '../lib/types'
export function RatingSummary({ reviews }: { reviews: Review[] }) { if (!reviews.length) return <div className="empty-inline">尚未收到项目评价</div>; const avg=(key:keyof Pick<Review,'skill_score'|'communication_score'|'delivery_score'|'time_management_score'>)=> (reviews.reduce((sum,r)=>sum+r[key],0)/reviews.length).toFixed(1); return <div className="rating-grid">{[['专业能力',avg('skill_score')],['沟通协作',avg('communication_score')],['成果交付',avg('delivery_score')],['时间管理',avg('time_management_score')]].map(([label,value])=><div key={label}><b>{value}</b><span>{label}</span></div>)}</div> }
